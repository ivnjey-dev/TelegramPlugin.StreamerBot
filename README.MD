# ✈️ Telegram Plugin for Streamer.bot

Высокопроизводительный плагин для отправки уведомлений, медиа и интерактивных меню в Telegram из Streamer.bot.

### 🚀 Возможности

* **Любой контент:** Текст, Фото, Видео.
* **Smart State:** Умная замена сообщений (виджеты, "сейчас играет", статусы стрима) без спама.
* **Топики:** Полная поддержка форумов и тредов (Message Threads).
* **Кнопки:** Inline-клавиатуры с поддержкой ссылок и кастомной сетки.
* **Автоматизация:** Возможность удаления исходных файлов (превью, клипы) после отправки.
* **Надежность:** Работает асинхронно, не блокирует выполнение, не отъедает потоки (что в последних версиях SB стало
  критичным и приводило к отмене задачи), кэширует состояние и переживает перезагрузки. Полностью потокобезопасен и
  может использоваться в нескольких экшонах одновременно.
* **Форматирование:** Поддерживает стандартный markdown (в данный момент V1) для форматирования сообщений.
* **Устойчивость:** Позволяет писать любые символы в сообщении, не ломая markdown форматирование, без ошибок отправки. имеет умную систему экранирования символов.

---

## 🛠️ Установка

1. Скачайте `TelegramPlugin.StreamerBot.dll` и файл импорта `TelegramPlugin.sb` из последнего **[Релиза на GitHub](https://github.com/ivnjey-dev/TelegramPlugin.StreamerBot/releases/latest)**.
2. Переместите `TelegramPlugin.StreamerBot.dll` в папку `dlls` внутри корневой папки Streamer.bot.
3. Импортируйте файл  `TelegramPlugin.sb` (шапка -> **Import**) перенести файл на поле ввода или копировать содержимое.
4. Следуйте примерам настройки и использования внутри импортированных экшонов и дочитайте документацию до конца.

<!-- TODO Добавить скриншот импорта -->

### Обязательные аргументы

Для работы нужен токен бота (от @BotFather) и id чата. Их можно задать один раз глобально или передавать в каждом экшоне.

| Аргумент       | Описание                                                                   |
|:---------------|:---------------------------------------------------------------------------|
| `tg_bot_token` | Токен вашего бота (например, `123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11`). |
| `tg_chat_id`   | ID чата или канала (напр. `-10012345678`).                                 |

---

## 📝 Список Аргументов

Управляйте установкой аргументов через **Core -> Arguments -> Set Argument** в поле Sub-Actions.

***Все нижеуказанные аргументы - опциональны.***

<!-- TODO Добавить скриншот настройки аргументов -->

### 📨 Основные

| Переменная        | Тип      | Описание                                               |
|:------------------|:---------|:-------------------------------------------------------|
| `tg_text`         | `string` | Текст сообщения. Поддерживает Markdown.                |
| `tg_topic_id`     | `int`    | ID топика (Thread ID) для форумов.                     |
| `tg_notification` | `bool`   | Показывать ли всплывающее уведомление в SB при успехе. |

### 🖼️ Медиа-файлы

| Переменная       | Тип      | Описание                                                                                                                                                                                                                        |
|:-----------------|:---------|:--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| `tg_media_path`  | `string` | Полный путь к файлу на диске.                                                                                                                                                                                                   |
| `tg_media_type`  | `string` | `Photo`, `Video`, `Text`. По умолчанию — `Auto` (плагин сам определит тип по расширению файла), но если указан конкретный, будет отсылать сообщения только этого типа (например если цель всегда отсылать ововещение с превью). |
| `tg_delete_file` | `bool`   | Удалить файл (клип, превью) с диска после успешной отправки.                                                                                                                                                                    |

### 🔄 Умное управление (Smart State)

Эти функции позволяют обновлять сообщения, создавая эффект "живых" виджетов или гибко управлять удалением предыдущих постов.

| Переменная       | Тип      | Описание                                                                                                                                               |
|:-----------------|:---------|:-------------------------------------------------------------------------------------------------------------------------------------------------------|
| `tg_state_key`   | `string` | Уникальный ID виджета (напр. `now_playing`). Сообщения с одинаковым ключом связаны.                                                                    |
| `tg_delete_prev` | `bool`   | Если `True` и задан ключ — удаляет предыдущее сообщение с этим ключом перед отправкой нового.                                                          |
| `tg_delete_all`  | `bool`   | **Осторожно!** Удаляет последние сообщение для каждого ключа, отправленные ранее с **любыми `tg_state_key`**. Плагин помнит их даже после перезапуска. |

> **Важно:** Если `tg_state_key` не задан, сообщение считается обычным и не заменяет другие сообщения (даже если
`tg_delete_prev` = True). Хотите заменять — придумайте ключ.

### 🔘 Кнопки (Inline Buttons)

Добавляйте кнопки-ссылки под сообщением. `N` — порядковый номер (0, 1, 2...).

| Переменная     | Описание                                                                                          |
|:---------------|:--------------------------------------------------------------------------------------------------|
| `tg_btn_textN` | Текст кнопки (напр. `tg_btn_text0` = "Twitch").                                                   |
| `tg_btn_urlN`  | Ссылка (напр. `tg_btn_url0` = "https://twitch.tv").                                               |
| `tg_layout`    | Схема расположения. Строка вида `"2, 1"`. <br>Значит: 2 кнопки в первом ряду, 1 кнопка во втором. |

---

## 💡 Примеры

### 1. Виджет "Сейчас играет"

Обновляемое сообщение с обложкой трека. Старое удаляется, новое появляется.

* `tg_chat_id`: `-100xxxxxx`
* `tg_text`: `🎵 %currentSong%`
* `tg_media_path`: `%coverPath%`
* `tg_state_key`: `music_widget`
* `tg_delete_prev`: `True`

### 2. Отправка клипа

Отправляет видео и удаляет файл с диска, чтобы не засорять память.

* `tg_text`: `Клип от %user%! :KEKW:`
* `tg_media_path`: `%clipFile%`
* `tg_delete_file`: `True`

### 3. Меню со ссылками

Сообщение с кнопками в два ряда.

* `tg_text`: `Подписывайтесь:`
* `tg_btn_text0`: `Twitch`
* `tg_btn_url0`: `https://twitch.tv/user`
* `tg_btn_text1`: `YouTube`
* `tg_btn_url1`: `https://youtube.com/user`
* `tg_btn_text2`: `Discord`
* `tg_btn_url2`: `https://discord.gg/invite`
* `tg_layout`: `2, 1` (Twitch и YouTube в ряд, Discord снизу)

---

### ⚠️ Примечания

1. **Регистр не важен:** В названии переменных можно писать `tg_chat_id`, `TG_CHAT_ID` или `Tg_ChaT_iD`. Плагин всё поймет.
2. **Fallback:** Если `tg_media_path` указан, но файл не найден — плагин отправит просто текст, чтобы не ломать
   оповещение. Если указан tg_media_type, но тип не совпадает, выдаст уведомление ошибки.
3. **Безопасность:** Плагин полностью потокобезопасен. Его можно без проблем использовать в нескольких экшенах
   одновременно.